# ============================================================
#  — API/.htaccess
# PHP source files here must never be served directly.
# They are only ever require_once'd by other PHP files.
# ============================================================

# Apache 2.4
Require all denied

# Apache 2.2 fallback
<IfModule !mod_authz_core.c>
  Order deny,allow
  Deny from all
</IfModule>

Options -Indexes
